#!/bin/bash
# microllm Container-Image bauen
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

echo "=== microllm Image bauen ==="
podman build -t microllm "$SCRIPT_DIR"
echo "Done: localhost/microllm"
